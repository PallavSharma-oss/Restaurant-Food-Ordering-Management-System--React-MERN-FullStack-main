name: Restaurant CI/CD Pipeline

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  checkout:

    name: Checkout Repository

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Verify Repository
        run: |
          pwd
          ls -la